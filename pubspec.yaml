name: screenshot_desktop
description: A pure Dart package for capturing high-resolution desktop screenshots on macOS and Windows, with multi-monitor support.
version: 1.0.0
homepage: https://github.com/kikuchy/screenshot_desktop
repository: https://github.com/kikuchy/screenshot_desktop

environment:
  sdk: ^3.10.3

platforms:
  windows:
  macos:

dependencies:
  objective_c: ^9.2.2
  hooks: ^0.20.5
  code_assets: ^0.19.10
  ffi: ^2.1.4
  win32: ^5.15.0
  logging: ^1.3.0
  native_toolchain_c: ^0.17.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  ffigen: ^20.1.1
  path: ^1.9.0
